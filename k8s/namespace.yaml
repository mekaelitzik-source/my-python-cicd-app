apiVersion: v1                         # Kubernetes API version for Namespace
kind: Namespace                        # Resource type: Namespace
metadata:                              # Metadata section contains name and labels
  name: cicd-app                       # Namespace name where the app will run
